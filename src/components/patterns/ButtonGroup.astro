---
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'div'>;

const { class: className, ...attrs } = Astro.props;
---

<div class:list={['flex flex-wrap items-center gap-2', className]} {...attrs}>
	<slot />
</div>
